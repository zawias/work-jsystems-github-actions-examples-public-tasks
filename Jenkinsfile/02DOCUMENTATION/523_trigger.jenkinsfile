pipeline {
    agent any
    triggers {
        pollSCM('H */4 * * *')
    }
    stages {
        stage('Build') {
            steps {
                echo 'Polling SCM every 4 hours'
            }
        }
    }
}
